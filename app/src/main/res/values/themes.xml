<resources>
    <style name="Theme.ScreenGuard" parent="@android:style/Theme.Material.Light.DarkActionBar" />

    <!-- 主畫面：全黑、無標題列，夜間開車不刺眼 -->
    <style name="Theme.ScreenGuard.Black" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:textColorSecondary">#BDBDBD</item>
    </style>

    <style name="SectionTitle">
        <item name="android:layout_marginTop">18dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
